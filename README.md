# 共识编年史 - GenLayer 链上多人游戏

## 📁 文件结构

```
genlayer-game/
├── api/
│   └── genlayer.js      # Vercel API 代理（解决CORS）
├── index.html           # 前端
├── vercel.json          # Vercel 配置
└── ConsensusChronicle.py # GenLayer 智能合约
```

## 🚀 部署步骤

### 1. 部署智能合约

1. 打开 https://studio.genlayer.com
2. 创建合约，粘贴 `ConsensusChronicle.py` 代码
3. 点击 Deploy
4. **复制合约地址**

### 2. 修改合约地址

打开 `index.html`，找到第 20 行：

```javascript
CONTRACT_ADDRESS: '0x4547e3E9742B4b4e6039e59C7219e40040cE',
```

替换成你的合约地址。

### 3. 部署到 Vercel

方法一：命令行
```bash
npm i -g vercel
cd genlayer-game
vercel
```

方法二：网页上传
1. 打开 vercel.com
2. New Project
3. 上传整个 `genlayer-game` 文件夹
4. Deploy

### 4. 开始游戏

- 分享 Vercel URL 给其他玩家
- 所有人必须用同一个合约地址
- 创建房间 → 等待玩家 → 开始游戏

## ⚠️ 重要

- `api/genlayer.js` 是代理服务器，解决浏览器 CORS 限制
- 所有游戏数据存储在 GenLayer 区块链上
- 多人同步通过 3 秒轮询实现
